var katzDeliLine = [];

function takeANumber(katzDeliLine, name) {
  katzDeliLine.push("name");
  return katzDeliLine;
  }
  
  function nowServing
  
  
  function currentLine(KatzDeliLine) {
    return "The line is currently:"${number}"."${name}","${number}"."${name}""
  }